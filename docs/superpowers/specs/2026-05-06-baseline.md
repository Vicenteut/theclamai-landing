# Baseline — Pre-redesign snapshot
**Date:** 2026-05-06
**URL:** https://theclamai.com
**Branch:** `redesign/v2` created from `main` at commit `9ac0b15`

## Stack

- Astro 6.2.2
- Tailwind CSS 4.2.4 (via `@tailwindcss/vite`)
- `@astrojs/cloudflare` adapter (deploys as Worker, not Pages)
- `@fontsource/inter` 5.2.8 (will be replaced by Geist Variable in Phase 1)
- `@astrojs/sitemap` 3.7.2
- Wrangler 4.88.0 (devDep)
- Node ≥ 22.12.0

## Production HTML snapshot

- Size: ~50 KB raw HTML
- Sections: 6 (`<section>` count)
- Visible word count: ~904
- Render: SSR via Cloudflare Workers (`cf-cache-status: HIT` on subsequent requests)
- `meta[name="generator"]`: Astro v6.2.2

## Existing components

```
src/components/
  FAQ.astro
  FinalCTA.astro
  Footer.astro
  Hero.astro
  HowItWorks.astro
  Navbar.astro
  Preloader.astro
  Pricing.astro
  Problem.astro          ← removed in redesign
  ProductPreview.astro   ← removed in redesign
  TrustStrip.astro       ← removed in redesign
```

## Pages

```
src/pages/
  index.astro
  thanks.astro
  legal/privacy.astro
  legal/terms.astro
```

No i18n. No `/es/` routes. All copy hardcoded inline in components.

## Lighthouse / a11y

Not measured locally (no headless Chrome in this VPS env). To be benchmarked at end of Phase 6 with Astro `astro preview` + remote run.

## Notes

- Inline styles dominate components (no Tailwind classes used despite Tailwind installed). The redesign moves to a token-based CSS file (`src/styles/tokens.css`) plus Tailwind utilities.
- Alpine.js is loaded via CDN inline `<script>` in `BaseLayout.astro` for FAQ accordion + mobile nav. Will keep this pattern.
- Single language (English only).
- No analytics / tracking installed.
- Brand accent color: `#d4af37` (gold). Decision pending whether to keep or move to pure white (Vercel-pure).
